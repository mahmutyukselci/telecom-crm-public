package com.telecom.customer_service.config.datasource;

/**
 * Enumeration representing available database datasource types for dynamic routing.
 */
public enum DataSourceType {
    MASTER,
    REPLICA
}
